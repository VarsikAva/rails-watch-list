<h1>New Bookmark</h1>

<%= simple_form_for [@list, @bookmark], url: "/lists/#{@list.id}/bookmarks/#{@bookmark.id}" do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Add Bookmark" %>
<% end %>

<%= link_to "Back to List", list_path(@list) %>
